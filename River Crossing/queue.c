/*
#include <stdlib.h>

typedef struct {
	WaitingRoom* prev;
	WaitingRoom* next;
	char v;
}WaitingRoom;

WaitingRoom joinWatingRoom(WaitingRoom* w,char type) {
	WaitingRoom* new_member = (WaitingRoom*)malloc(sizeof(WaitingRoom));
	new_member->next = w;
}
*/